[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vulnforge"
version = "3.1.0"
description = "A stdlib Python orchestrator for chained vulnerability discovery tooling."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "VulnForge contributors" }]
keywords = ["vulnerability", "security", "bug-bounty", "automation", "reconnaissance"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Security",
]
dependencies = []

[project.optional-dependencies]
progress = [
  "tqdm>=4.66",
]
ai = [
  "openai>=1.0",
  "anthropic>=0.30",
]
bot = [
  "aiohttp>=3.9",
]
all = [
  "tqdm>=4.66",
  "openai>=1.0",
  "anthropic>=0.30",
  "aiohttp>=3.9",
]
dev = [
  "pytest>=8",
  "pytest-cov>=5.0",
  "ruff>=0.5",
  "mypy>=1.10",
  "tqdm>=4.66",
]

[project.scripts]
vulnforge = "vulnforge:main"
reconchain = "vulnforge:main"

[tool.setuptools]
packages = ["vulnforge", "vulnforge.phases", "vulnforge.phases.recon", "vulnforge.cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "--tb=short -q"

[tool.coverage.run]
source = ["vulnforge"]
branch = true
omit = [
    "vulnforge/bot.py",
    "vulnforge/dashboard_server.py",
    "vulnforge/distributed.py",
    "tests/*",
]

[tool.coverage.report]
show_missing = true
fail_under = 5
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.",
    "raise NotImplementedError",
    "pass",
    "except ImportError",
    "except Exception:",
]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501", "F403", "F405"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = true
ignore_missing_imports = true
exclude = ["tests/", "phases/"]

